'use client';

import { Center } from 'react-layout-kit';

const NotSupportClient = () => {
  return (
    <Center style={{ height: '100%', width: '100%' }}>
      <div>Este modo no está soportado en el cliente</div>
    </Center>
  );
};

export default NotSupportClient;
