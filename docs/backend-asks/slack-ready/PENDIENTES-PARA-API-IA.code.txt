DE: COORD-AppEventos (FRONT chat-ia/appbodas)
PARA: API-IA
DRI: api-ia backend_oncall
ASUNTO: PENDIENTES de api-ia hacia chat-ia (consolidado 2026-06-11) — 3 abiertos

# ============================================================
# LO QUE NECESITAMOS DE API-IA — estado al 2026-06-11
# ============================================================

## ✅ YA CERRADO (no requiere acción, solo confirmación)
# RAG chat-ia migrado 3/3 verde (verificado COORD por curl, Qdrant):
#   /embed, /batch-embed-file (B1), /search → OK. rag.ts cableado.

## 🟡 PENDIENTE 1 — Fase 2 batch-embed-file con file_id PURO (sin file_url/text)
# En prod el front llama: {file_id, user_id} SIN file_url ni text.
# api-ia debe resolver file_url INTERNAMENTE a partir del file_id (R2).
# PREGUNTA: ¿con SOLO {file_id,user_id} ya resolvéis la URL en R2?
# Necesito saber QUÉ lookup hacéis (file_id -> url) para pasaros un file_id real de prueba.
$ curl -X POST https://api-ia.bodasdehoy.com/api/lobechat-kb/batch-embed-file \
    -H "X-Development: bodasdehoy" -d '{"file_id":"<REAL>","user_id":"<REAL>"}'
# => esperado: success + total_chunks (api-ia descarga de R2 por file_id)

## 🔴 PENDIENTE 2 — /chat/structured 502 (si sigue abierto)
# Reportado en F6/F7/F7.1/F7.2: /chat/structured daba 502 (root cause base_url Groq
# faltante en OpenAIClient → gsk_ a api.openai.com → 401 → 502).
# PREGUNTA: ¿está desplegado el fix de las 3 líneas (rest_chat_handler.py:6398 + openai.py:18)?
# Confirmad y re-corremos smoke /chat/structured.

## 🔴 PENDIENTE 3 — Billing en /chat/structured (bypass)
# F9: streaming /chat factura OK, pero /chat/structured BYPASEA los 4 módulos de billing
# (api_key_monitor + usage_tracking + cost_auditor + audit_alerts).
# PREGUNTA: ¿se integró el billing en /chat/structured? Toda feature IA debe medir+facturar.

# ------------------------------------------------------------
# NO bloqueante para nosotros pero conviene cerrar:
#   /query-embedding ya FUNCIONA (era bug, resuelto con Qdrant). OK.
# ------------------------------------------------------------

DRI: api-ia → responder estado de PENDIENTE 1/2/3 + fecha en este hilo.
